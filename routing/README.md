# Published routing data

The `guangzhou/manifest.json` file describes the currently published graph. Graph directories are immutable by `graphVersion`; consumers should use a jsDelivr URL pinned to a commit SHA.

Do not overwrite an existing graph version. Publish a new version and retain previous versions for cache-safe rollback.
